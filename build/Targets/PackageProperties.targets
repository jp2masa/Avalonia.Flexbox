<Project>

    <PropertyGroup>
        <PackageId Condition="'$(PackageId)' == ''">$(AssemblyName)</PackageId>
        <PackageId>jp2masa.$(PackageId)</PackageId>
        <PackageDescription Condition="'$(PackageDescription)' != ''">$(PackageDescription)

$(BaseDescription)</PackageDescription>
        <PackageDescription Condition="'$(PackageDescription)' == ''">$(BaseDescription)</PackageDescription>
    </PropertyGroup>

</Project>
